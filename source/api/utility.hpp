#pragma once

#include<string>
#include<cctype>
#include <algorithm>

class Utility {

private:

public:

	static int iequals(const std::string a, const std::string b);

	static std::string str_tolower(std::string s);
};